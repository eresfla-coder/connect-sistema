'use client'

/**
 * Painel Cliente — rota dedicada (/admin/painel-cliente).
 * Acesso principal pelo menu lateral (sem botão duplicado no hero da Central Admin).
 */
export { default } from '../cobranca/page'
